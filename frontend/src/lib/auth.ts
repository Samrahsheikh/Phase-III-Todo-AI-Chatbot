// frontend/src/lib/auth.ts
// Client-side only configuration for Better Auth
// NOTE: This is only for client-side usage, backend handles the actual authentication

// For client-side, we don't need database configuration
// The actual authentication is handled by the backend API

export const auth = {
  // Placeholder for client-side usage
  // Actual authentication is handled by backend API
  // This is just to prevent errors in client-side code
};