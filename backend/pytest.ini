[pytest]
python_files = test_*.py
addopts = --strict-markers --strict-config
filterwarnings =
    # ignore SQLModel deprecation warnings (expected due to SQLAlchemy 2.0 changes)
    ignore:The SQLModel module is using a deprecated API:DeprecationWarning
